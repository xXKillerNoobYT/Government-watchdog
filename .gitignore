# Raw run evidence and logs stay local/vault-only (Data publication boundary).
# Tools/scripts/contracts are versioned; raw gathered data and run logs are not.
Logs/
node_modules/
*.log

# macOS finder metadata (junk, never a deliverable)
.DS_Store

# Playwright-MCP scratch captures (transient run artifacts, not versioned evidence)
.playwright-mcp/
